
<%= render_if @highlights[0].present?, 'longer_featured', article: @highlights[0] %>

<div class="row mb-2">
    <%= render_if @highlights[1].present?, 'medium_featured', article: @highlights[1] %>
    <%= render_if @highlights[2].present?, 'medium_featured', article: @highlights[2] %>
</div>

<div class="row g-5">
    <div class="col-md-8">
        <h3 class="pb-4 mb-4 fst-italic border-bottom serif">
            Últimas Notícias
        </h3>
        <%= link_to "Novo Artigo", new_article_path, :class => 'btn btn-primary btn-sm px-3' %>
        <%= render @articles %>
        <%= render 'pagination' %>
    </div>
    <%= render 'right_side' %>
</div>
